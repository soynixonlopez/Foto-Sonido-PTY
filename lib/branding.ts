/**
 * Logo principal: PNG con canal alpha (fondo transparente).
 * Regenerar: `npm run logo-transparent` (tras cambiar el archivo) y si aplica `npm run trim-logo`.
 */
export const BRAND_LOGO = {
  src: "/images/logos/logoFotoSonido.png",
  width: 895,
  height: 283,
} as const;
